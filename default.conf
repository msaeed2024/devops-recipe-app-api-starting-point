http {  # <-- Add this block
    server {
        listen 80;
        server_name localhost;

        location / {
            proxy_pass http://app-1:8000;  # Adjust to match your app service
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
        }
    }
}